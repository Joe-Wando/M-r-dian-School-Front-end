# URL de l'API backend (voir architecture-technique.md)
VITE_API_URL=http://localhost:3001

# Tant que le backend n'est pas deploye, le frontend fonctionne sur une
# couche de donnees simulee (mocks). Passer a "false" une fois l'API en ligne.
VITE_USE_MOCKS=true
